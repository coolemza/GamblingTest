# GamblingTest

````
/home/leonid/IdeaProjects/GamblingTest$ java -jar build/libs/gamblingTest.jar --help
Options category 'misc':
  --alfa [-a] (a double; default: "1.2")
    Shape parameter.
  --beta [-b] (a double; default: "2.1")
    Scale parameter.
  --[no]help [-h] (a boolean; default: "true")
    Prints usage info.
  --length [-l] (an integer; default: "5")
    Sequnce length.